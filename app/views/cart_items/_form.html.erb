	
	<%= form_tag({:controller => "cart_items", :action => "create"}, method: "post",:class => "nifty_form") do %>
	<%= number_field_tag "cart_item[quantity]" %>
	<%= hidden_field_tag "cart_item[product_id]",product.id %>
	<%= hidden_field_tag "cart_item[cart_id]",@current_cart.id %>

	<div><%= submit_tag 'Add to cart' %></div>
	<% end %>